좋습니다. `LockFreeQueue.hpp`까지 오류가 0개라면 이제 **코드 작성 단계는 거의 끝났고, 통합 및 검증 단계**로 들어갑니다.

# PR-0005 남은 작업

```text
PR-0005
Scheduler Layer
```

---

## 1. Worker / Scheduler 변경사항 확인

```text
[ ] Worker.hpp
    LockFreeQueue<Task>
        ↓
    LockFreeQueue

[ ] Worker.cpp
    컴파일 오류 없는지 확인

[ ] Scheduler.hpp
    LockFreeQueue<Task>
        ↓
    LockFreeQueue

[ ] Scheduler.cpp
    make_unique<LockFreeQueue>()
```

※ 이 단계는 대부분 치환 확인만 하면 됩니다.

↓

## 2. CMakeLists.txt

```text
[ ] Scheduler source 추가

[ ] SchedulerTest target 추가

[ ] Threads::Threads 링크
```

↓

## 3. Build

프로젝트 루트:

```bash
cd ~/givemepi
```

Configure:

```bash
cmake -S . -B build
```

Build:

```bash
cmake --build build
```

확인:

```text
[ ] 컴파일 성공
[ ] 링크 성공
```

↓

## 4. SchedulerTest 실행

프로젝트 루트:

```bash
cd ~/givemepi
```

실행:

```bash
./build/tests/scheduler-test
```

또는 CMake에서 생성되는 실제 실행 파일 이름에 맞춰 실행.

확인:

```text
[ ] Scheduler OK 출력
[ ] Executed tasks: 100
```

↓

## 5. 문서 업데이트

```text
[ ] docs/CHECKLIST.md

[ ] docs/CHANGELOG.md

[ ] docs/DECISIONS.md
    (Queue를 Task 전용으로 변경한 설계 결정 반영)
```

↓

## 6. Git

```text
[ ] commit

[ ] push
```

↓

## 7. PR 완료

```text
PR-0005 완료
```

---

# 현재 우선순위

```text
1. Worker / Scheduler 헤더와 cpp가
   LockFreeQueue(Task 전용) 변경에 맞게
   오류 없이 수정되었는지 확인

↓

2. CMakeLists.txt

↓

3. Build
```

다음으로 진행할 파일은 **`CMakeLists.txt`** 입니다. 여기서 Scheduler 관련 소스와 테스트 타깃을 프로젝트에 연결하면 빌드 검증 단계로 넘어갈 수 있습니다.
